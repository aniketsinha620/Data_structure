   for (const auto& combination : s) {
            cout << "[ ";
            for (const auto& num : combination) {
                cout << num << " ";
            }
            cout << "]" << endl;
        }